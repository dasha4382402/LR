С#
using System;

static void Add()
    {
        var i = 2 +3;
    }

IL
         IL_0000: nop
        IL_0001: ldc.i4.5
        IL_0002: stloc.0
        IL_0003: ret
        Разбор
        1. IL_0000: nop
   - nop (no operation) — это команда, которая не выполняет никаких действий. Она используется как "заполнитель" и может быть полезна для отладки или для создания структуры кода. В данном случае, этот код фактически ничего не делает, просто занимает место.

2. IL_0001: ldc.i4.5
   - ldc.i4.5 — это команда, которая загружает константу 5 в стек. Эта команда означает "Load Constant Integer 4 bytes", и в данном случае она загружает целое число 5. По сути, она помещает значение 5 в стек, чтобы оно могло быть использовано в последующих инструкциях.

3. IL_0002: stloc.0
   - stloc.0 — это команда, которая хранит значение, которое находится на вершине стека, в локальной переменной с индексом 0. Этот код предполагает, что мы уже загрузили число 5 в стек, и теперь мы сохраняем его в локальной переменной. Индекс 0 чаще всего означает, что это первая локальная переменная метода.

4. IL_0003: ret
   - ret — команда, которая завершает выполнение метода и возвращает управление вызывающему коду. Так как здесь не указано значение для возвращения, метод, вероятно, возвращает void (ничего).
