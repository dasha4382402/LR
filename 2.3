Целые числа (int, long, byte) хранятся в двоичной форме. Размер может варьироваться (1 байт, 2 байта, 4 байта). Например, int обычно занимает 4 байта (32 бита). 

Вещественные числа (float, double) хранятся в формате с плавающей точкой. Обычно по стандарту IEEE 754. float обычно занимает 4 байта (32 бита), а double — 8 байтов (64 бита). 

Строки хранятся в памяти в виде последовательностей байтов. Например, в кодировке ASCII каждый символ занимает 1 байт, в кодировке UTF-8 может занимать от 1 до 4 байтов.
